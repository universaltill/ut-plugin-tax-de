# Code review: adopt plugin i18n locale-drift guard (ut-docs#1882)

**Date:** 2026-09-10
**Author:** Pouria Teimouri (`lane:cloud-41`), implemented inline (Sonnet, `complexity:medium`)
**Reviewer:** independent Opus subagent (fresh context, no shared context with the author)

## What shipped

Copied `scripts/guard-plugin-i18n.sh` from the reusable template landed in
`ut-docs#1882` (`ut-docs/scripts/templates/guard-plugin-i18n.sh`) and added
one new CI step (`.github/workflows/ci.yml`, after `Validate manifest +
plugin manifest`, before `Package (dry run)`).

This repo has no `locales/` directory today, so the new step is a clean
no-op — wired in now so drift is caught from the first key this plugin
ever ships, per ut-docs#1882's own design goal.

## What the independent review found

Full review covered the template itself plus both adopting repos in one
pass — see `ut-docs`'s own record,
`code-reviews/2026-09-10-plugin-i18n-guard-template-1882.md`, for the
complete finding list (four fixed: nested-value rejection, missing
self-test CI wiring, dropped verb-parity check, raw tracebacks on
malformed input; two nits left as-is). Nothing specific to this repo's
adoption diff (a two-file change: copy the script, add one CI step) — the
review confirmed the copy is byte-identical to the fixed template and the
new step is placed correctly (no `working-directory` override, runs from
repo root, doesn't disturb the existing `validate.sh`/`package.sh` steps
around it).

## Verification

- `bash scripts/guard-plugin-i18n.sh` run directly against this repo —
  `no locales/ directory — nothing to check`, exit 0.
- `gofmt -l .` — clean (no Go files touched by this change).
- Diffed against `ut-docs`'s template post-review-fixes — byte-identical.
